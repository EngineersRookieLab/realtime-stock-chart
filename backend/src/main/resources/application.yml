spring:
  application:
    name: realtime-stock-chart
  datasource:
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://localhost:5432/postgres
    username: postgres
    password: 1111
  jpa:
    hibernate:
    ddl-auto: update
    show-sql: true
    database: postgresql
